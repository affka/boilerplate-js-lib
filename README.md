# Boilerplate JS/SPA Library
Boilerplate for create JavaScript library or Single Page Application

## Included dependencies (by default)
* underscore
* jquery
* when (Promise)
* neatness

# Install
* Replace `mylib` to you project name
* Run commands:

```sh
    npm install
    bower install
```